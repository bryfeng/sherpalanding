---
// Hero section with execution-focused messaging
---

<section class="hero" id="hero">
  <div class="hero-content">
    <span class="eyebrow">DeFi Execution Engine</span>
    <h1>Execute DeFi Strategies.<br /><span class="text-gradient">Automatically.</span></h1>
    <p class="hero-subtitle">
      Set up DCA schedules, automated swaps, and risk policies. Sherpa executes with guardrails you control.
    </p>
    <div class="hero-cta">
      <a class="btn btn-primary btn-lg" href="https://app.runsherpa.ai">Launch App</a>
      <a class="btn btn-secondary btn-lg" href="#how">See how it works</a>
    </div>
  </div>

  <div class="hero-visual">
    <div class="hero-demo animate-float">
      <div class="demo-card demo-main">
        <div class="demo-card-header">
          <span class="demo-card-title">Active Strategy</span>
          <span class="demo-status">Running</span>
        </div>
        <p class="demo-value">Weekly ETH DCA</p>
        <p class="demo-helper">$100 USDC → ETH every Monday</p>
        <div class="demo-progress">
          <div class="demo-progress-bar"></div>
        </div>
        <p class="demo-meta">Next execution in 2d 14h</p>
      </div>
      <div class="demo-grid">
        <div class="demo-card demo-small">
          <div class="demo-card-header">
            <span class="demo-card-title">Guardrails</span>
            <span class="demo-status demo-status-active">Active</span>
          </div>
          <p class="demo-helper">Max slippage: 1.5%</p>
          <p class="demo-helper">Gas limit: $10</p>
        </div>
        <div class="demo-card demo-small">
          <div class="demo-card-header">
            <span class="demo-card-title">Total Executed</span>
          </div>
          <p class="demo-value-small">$4,200</p>
          <p class="demo-helper">12 transactions</p>
        </div>
      </div>
    </div>
    <div class="hero-glow"></div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
    min-height: 100vh;
    padding: calc(80px + var(--space-16)) var(--space-6) var(--space-16);
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .hero-content {
    animation: slide-up var(--duration-slow) var(--ease-out);
  }

  .eyebrow {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: var(--space-4);
  }

  .hero h1 {
    font-size: var(--text-5xl);
    font-weight: 600;
    line-height: var(--leading-tight);
    margin-bottom: var(--space-6);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--text-muted);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-8);
    max-width: 480px;
  }

  .hero-cta {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  /* Visual / Demo Cards */
  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
    animation: fade-in var(--duration-slower) var(--ease-out);
    animation-delay: 200ms;
    animation-fill-mode: both;
  }

  .hero-demo {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    width: 100%;
    max-width: 400px;
  }

  .hero-glow {
    position: absolute;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, var(--accent-muted) 0%, transparent 70%);
    filter: blur(60px);
    z-index: -1;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .demo-card {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    padding: var(--space-5);
    transition: all var(--duration-normal) var(--ease-out);
  }

  .demo-card:hover {
    border-color: var(--color-line-strong);
    box-shadow: var(--shadow-md);
  }

  .demo-main {
    background: var(--surface-2);
  }

  .demo-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-3);
  }

  .demo-card-title {
    font-size: var(--text-xs);
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  .demo-status {
    font-size: var(--text-2xs);
    font-weight: 600;
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-full);
    background: var(--color-success-muted);
    color: var(--color-success);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  .demo-status-active {
    background: var(--accent-muted);
    color: var(--accent);
  }

  .demo-value {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: 600;
    color: var(--text);
    margin-bottom: var(--space-1);
  }

  .demo-value-small {
    font-family: var(--font-mono);
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--text);
    margin-bottom: var(--space-1);
  }

  .demo-helper {
    font-size: var(--text-sm);
    color: var(--text-muted);
  }

  .demo-progress {
    height: 4px;
    background: var(--surface-3);
    border-radius: var(--radius-full);
    margin: var(--space-4) 0 var(--space-2);
    overflow: hidden;
  }

  .demo-progress-bar {
    width: 65%;
    height: 100%;
    background: linear-gradient(90deg, var(--accent), var(--color-secondary));
    border-radius: var(--radius-full);
  }

  .demo-meta {
    font-size: var(--text-xs);
    color: var(--text-subtle);
  }

  .demo-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
  }

  .demo-small {
    padding: var(--space-4);
  }

  @media (max-width: 968px) {
    .hero {
      grid-template-columns: 1fr;
      gap: var(--space-12);
      text-align: center;
      padding-top: calc(80px + var(--space-12));
    }

    .hero-subtitle {
      max-width: none;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-cta {
      justify-content: center;
    }

    .hero-visual {
      order: -1;
    }

    .hero-demo {
      max-width: 360px;
    }
  }

  @media (max-width: 480px) {
    .hero h1 {
      font-size: var(--text-4xl);
    }

    .hero-cta {
      flex-direction: column;
    }

    .hero-cta .btn {
      width: 100%;
    }

    .demo-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
